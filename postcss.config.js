// PostCSS — инструмент, который обрабатывает CSS после написания.
// Autoprefixer — плагин, который автоматически добавляет -webkit-, -moz- и другие
// вендорные префиксы в CSS, чтобы стили работали во всех браузерах.

export default {
  plugins: {
    autoprefixer: {},
  },
}
